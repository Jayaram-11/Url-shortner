// Loader component shown during API calls.
// Displayed inside buttons or as a standalone spinner.

function Loader() {
  return <span className="loader-text">Loading...</span>;
}

export default Loader;
